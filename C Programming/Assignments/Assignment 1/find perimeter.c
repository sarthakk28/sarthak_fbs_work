//Write a C program to input the length and width of a rectangle and find its perimeter.
void main()
{
  int length = 15,width = 24,perimeter;
  
  perimeter = 2*(length+width);

  printf("perimeter is : %d",perimeter);
  return 0;  
}